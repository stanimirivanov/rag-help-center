replicaCount: 1

image:
  registry: fra.ocir.io
  namespace: <set-via-deployment>
  name: gateway
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
