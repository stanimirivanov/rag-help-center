apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-admin-api
spec:
  selector:
    app: admin-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
