apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-embedding-worker
spec:
  selector:
    app: embedding-worker
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
