apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-qa-service
spec:
  selector:
    app: qa-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
