replicaCount: 1

image:
  registry: fra.ocir.io
  namespace: <set-via-deployment>
  name: retrieval-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8081

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
